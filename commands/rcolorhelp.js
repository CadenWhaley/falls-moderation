const db = require("quick.db");
const Discord = require('discord.js');

module.exports = {
    name: 'rcolorhelp',
    description: "",
    execute(message, args){
      message.channel.send("shorturl.at/dsM05")
      message.channel.send("This is a shortened URL not a bad one DM me if you feel unsafe about it. Also its just a hex code picker so you dont have to look one up.")
    }
}



